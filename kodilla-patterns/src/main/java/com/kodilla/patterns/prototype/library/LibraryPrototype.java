package com.kodilla.patterns.prototype.library;

public class LibraryPrototype implements Cloneable {
    @Override
    protected Object clone() throws CloneNotSupportedException {
        return super.clone();
    }
}
